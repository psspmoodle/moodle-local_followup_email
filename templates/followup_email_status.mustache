<div class="table-responsive mt-3">
    <table class="table">
        <thead>
        <tr>
            <th class="font-weight-bold">{{user}}</th>
            <th class="font-weight-bold">{{event}}</th>
            <th class="font-weight-bold">{{datetobesent}}</th>
            <th class="font-weight-bold">{{emailsent}}</th>
        </tr>
        </thead>
        {{#rows}}
            <tr>
                <td>{{fullname}}</td>
                <td>{{completion_time}}</td>
                <td>{{email_to_be_sent}}</td>
                <td>{{email_time_sent}}</td>
            </tr>
        {{/rows}}
    </table>
</div>
<a class="btn btn-light" href="{{indexurl}}">
    {{returntext}}
</a>